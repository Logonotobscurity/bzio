# =====================================================
# VERCEL PRODUCTION ENVIRONMENT CONFIGURATION
# =====================================================
# This file provides FALLBACK values for production.
# PRIMARY CONFIGURATION: Use Vercel Dashboard > Settings > Environment Variables
# 
# Priority Order:
# 1. Vercel Environment Variables (Highest Priority)
# 2. Values in this file (.env.production)
# 3. Default values in code (Lowest Priority)
#
# See: VERCEL_DEPLOYMENT_GUIDE.md for complete instructions

# =====================================================
# CRITICAL: Authentication
# =====================================================
# ⚠️ WARNING: These MUST be set in Vercel Dashboard, NOT in this file
# For security, sensitive values are stored in Vercel secrets only
# See: VERCEL_DEPLOYMENT_GUIDE.md for complete setup instructions

# The deployed URL - MUST match your Vercel domain
# Set in Vercel Dashboard: NEXTAUTH_URL=https://YOUR_VERCEL_URL.vercel.app
# Update after Vercel deployment
NEXTAUTH_URL=https://bzion-shop.vercel.app

# NextAuth Secret - DO NOT set here, ONLY in Vercel Dashboard
# ⚠️ CRITICAL: Must be 32+ characters, cryptographically secure random value
# Generate with: openssl rand -hex 32
# Set in Vercel Dashboard > Settings > Environment Variables
# This file uses a placeholder for local builds only
NEXTAUTH_SECRET=placeholder_for_local_builds_only_use_vercel_dashboard_for_production

# =====================================================
# DATABASE (Prisma)
# =====================================================
# Database connection string - Set in Vercel Dashboard for security
# ⚠️ CRITICAL SECRET: Never commit the actual connection string
# Format: postgres://user:password@host:port/database?sslmode=require
# Set in Vercel Dashboard > Settings > Environment Variables
# This file uses a placeholder for local builds only
DATABASE_URL=placeholder_for_local_builds_only_use_vercel_dashboard_for_production

# =====================================================
# EMAIL SERVICE (Resend SMTP)
# =====================================================
EMAIL_SERVER_HOST="smtp.resend.com"
EMAIL_SERVER_PORT="587"
EMAIL_SERVER_USER="resend"
# ⚠️ CRITICAL SECRET: Resend API Key / SMTP Password
# DO NOT set here, ONLY in Vercel Dashboard
# Set in Vercel Dashboard > Settings > Environment Variables
# This file uses a placeholder for local builds only
EMAIL_SERVER_PASSWORD="placeholder_for_local_builds_only_use_vercel_dashboard_for_production"
EMAIL_FROM="BZION <noreply@bzion.shop>"

# =====================================================
# CACHING & RATE LIMITING (Upstash Redis)
# =====================================================
# Upstash Redis REST URL - Public (safe to commit)
UPSTASH_REDIS_REST_URL="https://quality-slug-43912.upstash.io"
# ⚠️ CRITICAL SECRET: Upstash Redis Token
# DO NOT set here, ONLY in Vercel Dashboard
# Set in Vercel Dashboard > Settings > Environment Variables
# This file uses a placeholder for local builds only
UPSTASH_REDIS_REST_TOKEN="placeholder_for_local_builds_only_use_vercel_dashboard_for_production"

# =====================================================
# PUBLIC CONFIGURATION (Safe to commit)
# =====================================================
# WhatsApp Business Integration
NEXT_PUBLIC_WHATSAPP_BUSINESS_PHONE="+2347010326015"
WHATSAPP_BUSINESS_NUMBER="+2347010326015"
WHATSAPP_BUSINESS_URL="https://wa.me/message/TOVLTP6EMAWNI1"

# Monitoring & Versioning
NEXT_PUBLIC_APP_VERSION="1.0.0"

# Feature Flags
DATA_SOURCE="static"

# =====================================================
# BUILD CONFIGURATION
# =====================================================
# These are set in netlify.toml, but can be overridden here
NODE_ENV="production"
NODE_VERSION="20"

# =====================================================
# NOTES
# =====================================================
# 1. Replace all placeholder values in Vercel Dashboard
# 2. Never commit actual secrets to version control
# 3. Vercel secrets are encrypted and never exposed in build logs
# 4. For local development, use .env file instead
# 5. Test production build locally with: cross-env NODE_ENV=production next build
# 6. After Vercel deployment, update NEXTAUTH_URL to match your Vercel domain
