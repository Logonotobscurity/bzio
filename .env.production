# =====================================================
# NETLIFY PRODUCTION ENVIRONMENT CONFIGURATION
# =====================================================
# This file provides FALLBACK values for production.
# PRIMARY CONFIGURATION: Use Netlify UI > Site Settings > Build & Deploy > Environment
# 
# Priority Order:
# 1. Netlify UI Environment Variables (Highest Priority)
# 2. Values in this file (.env.production)
# 3. Default values in code (Lowest Priority)
#
# See: NETLIFY_ENVIRONMENT_SETUP.md for complete instructions

# =====================================================
# CRITICAL: Authentication
# =====================================================
# ⚠️ WARNING: These MUST be set in Netlify UI, NOT in this file
# For security, sensitive values are stored in Netlify secrets only
# See: NETLIFY_SECRETS_CONTROLLER_GUIDE.md for complete setup instructions

# The deployed URL - MUST match your Netlify domain
# Set in Netlify UI: NEXTAUTH_URL=https://bzionshopfmcg.netlify.app
# Required value: https://bzionshopfmcg.netlify.app
NEXTAUTH_URL=https://bzionshopfmcg.netlify.app

# NextAuth Secret - DO NOT set here, ONLY in Netlify UI
# ⚠️ CRITICAL: Must be 32+ characters, cryptographically secure random value
# Generate with: openssl rand -hex 32
# Set in Netlify UI with "Contains secret values" toggle ENABLED
# This file uses a placeholder for local builds only
NEXTAUTH_SECRET=placeholder_for_local_builds_only_use_netlify_ui_for_production

# =====================================================
# DATABASE (Prisma)
# =====================================================
# Database connection string - Set in Netlify UI for security
# ⚠️ CRITICAL SECRET: Never commit the actual connection string
# Format: postgres://user:password@host:port/database?sslmode=require
# Set in Netlify UI with "Contains secret values" toggle ENABLED
# This file uses a placeholder for local builds only
DATABASE_URL=placeholder_for_local_builds_only_use_netlify_ui_for_production

# =====================================================
# EMAIL SERVICE (Resend SMTP)
# =====================================================
EMAIL_SERVER_HOST="smtp.resend.com"
EMAIL_SERVER_PORT="587"
EMAIL_SERVER_USER="resend"
# ⚠️ CRITICAL SECRET: Resend API Key / SMTP Password
# DO NOT set here, ONLY in Netlify UI
# Set in Netlify UI with "Contains secret values" toggle ENABLED
# This file uses a placeholder for local builds only
EMAIL_SERVER_PASSWORD="placeholder_for_local_builds_only_use_netlify_ui_for_production"
EMAIL_FROM="BZION <noreply@bzion.shop>"

# =====================================================
# CACHING & RATE LIMITING (Upstash Redis)
# =====================================================
# Upstash Redis REST URL - Public (safe to commit)
UPSTASH_REDIS_REST_URL="https://quality-slug-43912.upstash.io"
# ⚠️ CRITICAL SECRET: Upstash Redis Token
# DO NOT set here, ONLY in Netlify UI
# Set in Netlify UI with "Contains secret values" toggle ENABLED
# This file uses a placeholder for local builds only
UPSTASH_REDIS_REST_TOKEN="placeholder_for_local_builds_only_use_netlify_ui_for_production"

# =====================================================
# PUBLIC CONFIGURATION (Safe to commit)
# =====================================================
# WhatsApp Business Integration
NEXT_PUBLIC_WHATSAPP_BUSINESS_PHONE="+2347010326015"
WHATSAPP_BUSINESS_NUMBER="+2347010326015"
WHATSAPP_BUSINESS_URL="https://wa.me/message/TOVLTP6EMAWNI1"

# Monitoring & Versioning
NEXT_PUBLIC_APP_VERSION="1.0.0"

# Feature Flags
DATA_SOURCE="static"

# =====================================================
# BUILD CONFIGURATION
# =====================================================
# These are set in netlify.toml, but can be overridden here
NODE_ENV="production"
NODE_VERSION="20"

# =====================================================
# NOTES
# =====================================================
# 1. Replace all "placeholder_set_in_netlify_ui" values in Netlify UI
# 2. Never commit actual secrets to version control
# 3. Netlify secrets are encrypted and never exposed in build logs
# 4. For local development, use .env file instead
# 5. Test production build locally with: cross-env NODE_ENV=production next build
