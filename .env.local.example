# =====================================================
# LOCAL DEVELOPMENT ENVIRONMENT (.env.local)
# =====================================================
# This file is for LOCAL DEVELOPMENT ONLY
#
# SETUP INSTRUCTIONS:
# 1. Copy this file to .env.local (git-ignored)
# 2. Update values with your LOCAL development settings
# 3. IMPORTANT: Use test/fake values, NOT production secrets
# 4. NEVER commit .env.local to git (it's in .gitignore)
# 5. When you pull latest code, production secrets come from Netlify UI, not this file
#
# For production, use Netlify UI environment variables instead.
# See: NETLIFY_SECRETS_CONTROLLER_GUIDE.md

# =====================================================
# Authentication
# =====================================================
# Use localhost for local development
NEXTAUTH_URL=http://localhost:3000
# Use any random string for local testing (not sensitive locally)
# Generate: openssl rand -hex 32 (or just use a test string)
NEXTAUTH_SECRET=test_secret_for_local_development_only_change_for_production_min_32_chars
AUTH_URL=http://localhost:3000

# =====================================================
# DATABASE (Prisma)
# =====================================================
# Use your local or cloud database connection for testing
# Format: postgres://user:pass@host:port/database
# Examples:
#   Local: postgres://postgres:password@localhost:5432/bzion_dev
#   Cloud: postgres://user:password@db.provider.com:5432/database?sslmode=require
DATABASE_URL="postgres://user:password@localhost:5432/bzion_dev"

# =====================================================
# EMAIL SERVICE (Resend SMTP)
# =====================================================
# For local development, you can use:
# - Resend (recommended): Get token from https://resend.com
# - Ethereal (fake SMTP): Get temp account from https://ethereal.email
# - Local Mailhog: Run `docker run -p 1025:1025 -p 8025:8025 mailhog/mailhog`
# - Mock: Use a fake/test API key for testing

EMAIL_SERVER_HOST="smtp.resend.com"
EMAIL_SERVER_PORT="587"
EMAIL_SERVER_USER="resend"
# LOCAL: Use test API key or fake value (not production secret)
EMAIL_SERVER_PASSWORD="test_resend_api_key_or_dummy_value_not_production"
EMAIL_FROM="BZION Dev <dev@localhost>"

# =====================================================
# CACHING & RATE LIMITING (Upstash Redis)
# =====================================================
# For local development, you can use:
# - Upstash (recommended): https://upstash.com (free tier available)
# - Local Redis: Run `docker run -p 6379:6379 redis/redis-alpine`
# - Disable for local dev by setting to empty string (optional)

# LOCAL: Use test credentials (not production secret)
UPSTASH_REDIS_REST_URL="https://test-upstash-instance.upstash.io"
UPSTASH_REDIS_REST_TOKEN="test_upstash_token_or_dummy_value_not_production"

# =====================================================
# WhatsApp Integration
# =====================================================
# Business phone and messaging URL
NEXT_PUBLIC_WHATSAPP_BUSINESS_PHONE="+2347010326015"
NEXT_WHATSAPP_BUSINESS_URL="https://wa.me/message/YOUR_MESSAGE_ID"

# =====================================================
# Monitoring & Features
# =====================================================
NEXT_PUBLIC_APP_VERSION="1.0.0-dev"
DATA_SOURCE="static"

# =====================================================
# WhatsApp Business Setup
# =====================================================
WHATSAPP_BUSINESS_NUMBER="+2347010326015"
WHATSAPP_BUSINESS_URL="https://wa.me/message/YOUR_MESSAGE_ID"

# =====================================================
# Optional: Debugging
# =====================================================
# Uncomment to enable debug logging
# DEBUG=*
# NEXT_PUBLIC_DEBUG=true

# =====================================================
# NODE CONFIGURATION
# =====================================================
NODE_ENV="development"
NODE_VERSION="20"

# =====================================================
# IMPORTANT SECURITY NOTES
# =====================================================
# 1. NEVER commit this file (.env.local) to Git
#    - It's in .gitignore to prevent accidental commits
#    - Each developer has their own .env.local with test values
#
# 2. NEVER use production secrets here
#    - Use test/fake API keys for development
#    - Production secrets only exist in Netlify UI
#
# 3. When pulling latest code:
#    - .env.local is NOT updated from Git
#    - Production secrets stay in Netlify UI only
#    - Your local secrets remain unchanged
#
# 4. For production deployment:
#    - Set all variables in Netlify UI
#    - Mark sensitive ones with "Contains secret values"
#    - See NETLIFY_SECRETS_CONTROLLER_GUIDE.md
