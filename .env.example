# =====================================================
# ENVIRONMENT VARIABLES TEMPLATE
# =====================================================
# This file serves as a template for all required environment variables.
# 
# FOR LOCAL DEVELOPMENT:
#   1. Copy this file to .env.local
#   2. Fill in YOUR values with test/development credentials
#   3. NEVER commit .env.local to Git (it's in .gitignore)
#   4. Use fake/test values, NOT production secrets
#
# FOR PRODUCTION (Netlify):
#   1. DO NOT set secrets in this file or netlify.toml
#   2. Set all variables in Netlify UI > Site Settings > Build & Deploy > Environment
#   3. Mark sensitive values with "Contains secret values" toggle
#   4. See: NETLIFY_SECRETS_CONTROLLER_GUIDE.md for detailed instructions
#
# SECURITY: DO NOT COMMIT sensitive values to git!

# =====================================================
# Authentication (NextAuth v4)
# =====================================================
# For local: http://localhost:3000
# For production: https://bzionshopfmcg.netlify.app
NEXTAUTH_URL=http://localhost:3000

# Secret key for JWT - Generate a random 32+ character string
# PRODUCTION: Set in Netlify UI with "Contains secret values" toggle ENABLED
# LOCAL DEV: Use any random string for testing (not sensitive locally)
# Generate: openssl rand -hex 32
NEXTAUTH_SECRET=your_secure_random_string_here_min_32_chars_for_testing

# Authentication URL
AUTH_URL=http://localhost:3000

# =====================================================
# Database (Prisma + PostgreSQL)
# =====================================================
# Format: postgres://user:password@host:port/database?sslmode=require
# Contact database provider for connection string
# PRODUCTION: Set in Netlify UI with "Contains secret values" toggle ENABLED
# LOCAL DEV: Use your local database URL
DATABASE_URL=postgres://user:password@host:port/database?sslmode=require

# =====================================================
# Email Service (Resend SMTP)
# =====================================================
# SMTP Server Configuration for sending emails
EMAIL_SERVER_HOST=smtp.resend.com
EMAIL_SERVER_PORT=587
EMAIL_SERVER_USER=resend
# PRODUCTION: Set in Netlify UI with "Contains secret values" toggle ENABLED
# LOCAL DEV: Use your Resend API key or fake value for testing
EMAIL_SERVER_PASSWORD=your_resend_api_key_or_test_value_here
EMAIL_FROM="BZION Shop <noreply@bzion.shop>"

# =====================================================
# Caching & Rate Limiting (Upstash Redis)
# =====================================================
# Redis server for caching and rate limiting
# PRODUCTION: Set in Netlify UI with "Contains secret values" toggle ENABLED
# LOCAL DEV: Use your Upstash instance or local Redis
UPSTASH_REDIS_REST_URL=https://your-upstash-instance.upstash.io
UPSTASH_REDIS_REST_TOKEN=your_upstash_token_or_test_value_here

# =====================================================
# WhatsApp Integration
# =====================================================
# Business phone number and messaging URL
NEXT_PUBLIC_WHATSAPP_BUSINESS_PHONE=+2347010326015
NEXT_WHATSAPP_BUSINESS_URL=https://wa.me/message/YOUR_MESSAGE_ID

# =====================================================
# Monitoring & Version
# =====================================================
# Application version (visible in frontend)
NEXT_PUBLIC_APP_VERSION=1.0.0

# Data source mode (static or dynamic)
DATA_SOURCE=static

# =====================================================
# WhatsApp Business Integration
# =====================================================
WHATSAPP_BUSINESS_NUMBER=+2347010326015
WHATSAPP_BUSINESS_URL=https://wa.me/message/YOUR_MESSAGE_ID

# =====================================================
# Optional: Development Debugging
# =====================================================
# Uncomment these for local development only
# DEBUG=*
# NEXT_PUBLIC_DEBUG=true

# =====================================================
# FEATURE FLAGS
# =====================================================
DATA_SOURCE="static"  # Options: static, dynamic, hybrid

# =====================================================
# BUILD CONFIGURATION (Usually set by build system)
# =====================================================
NODE_ENV="development"
NODE_VERSION="20"

# =====================================================
# INSTRUCTIONS FOR SETUP
# =====================================================
# 
# LOCAL DEVELOPMENT:
# 1. Copy this file: cp .env.example .env.local
# 2. Replace YOUR_* values with actual test/dev credentials
# 3. IMPORTANT: Use test values, never production secrets locally
# 4. Never commit .env.local to version control
# 5. Run: npm run dev
#
# PRODUCTION (Netlify):
# 1. Go to netlify.com > your site > Project configuration > Environment
# 2. Add each variable from this file
# 3. For sensitive variables (NEXTAUTH_SECRET, DATABASE_URL, etc.):
#    - Enable "Contains secret values" toggle
#    - Netlify will encrypt and protect these
# 4. Push to main branch to trigger production deploy
# 5. See NETLIFY_SECRETS_CONTROLLER_GUIDE.md for complete setup
#
# SECURITY CHECKLIST:
# ✅ Never commit .env files with real secrets
# ✅ Use process.env.VARIABLE_NAME in code
# ✅ Mark all secrets in Netlify UI
# ✅ Test locally before production deploy
# ✅ Monitor Netlify build logs for secret exposure
# 5. Generate secrets with: openssl rand -base64 32

